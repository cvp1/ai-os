   Keep every skill read-by-default and write/send-gated behind my explicit OK.

5) MAIL & CALENDAR WIRING — LOCAL-FIRST (stay responsive, avoid rate limits)
   This works the SAME way whichever backend I picked in the interview; only the
   connector and the "what changed?" mechanism differ. Use cache/<backend>/ — i.e.
   cache/m365/ for Microsoft 365, cache/google/ for Google Workspace (both dirs if I
   use both).
   - Confirm the right connector is connected. These first-party connectors are turned
     on from my Claude account (claude.ai → Settings → Connectors) and authorized via
     OAuth — do NOT try to add them locally with `claude mcp add` or `claude mcp login`
     (those are for self-hosted servers, not these account connectors). You can check
     whether a connector is signed in from inside this session by running /mcp; if one
     needs re-authorizing, recent Claude Code flags it when the session starts and points
     you at /mcp:
       • Microsoft 365 → Outlook mail + calendar (Teams, SharePoint, OneDrive if available).
         The connector is read-only — which is all this setup needs, since I draft
         everything locally and send it myself, so M365 is fully capable here, not a
         lesser path than Google. The real constraint is ACCESS, not capability: it
         requires a work / Microsoft 365 (Entra) account — a PERSONAL @outlook.com or
         @hotmail.com address cannot connect at all. On many tenants I can approve the
         connection myself when I sign in; some still require a one-time admin consent. If
         a work account won't authorize, tell me I may need my IT admin —
         before I sink time into the build.
       • Google Workspace → Gmail + Google Calendar (Drive if available) — connects
         self-serve for an individual, personal Gmail included.
     If it isn't connected — or /mcp shows it needs sign-in — point me to claude.ai →
     Settings → Connectors (or run /mcp) to authorize it; once I re-authorize, Claude
     reconnects on its own. Then verify by reading my next 3 calendar events and my
     latest unread subject lines (read-only).

   - LOCAL CACHE IS THE DEFAULT READ PATH. These APIs rate-limit and round-trips add
     latency, so do NOT hit them on every question. Maintain a local mirror under
     cache/<backend>/:
       cache/<backend>/calendar.json   upcoming + recent events
       cache/<backend>/mail/           recent message headers + bodies I've opened
       cache/<backend>/contacts.json   resolved people (the folks I work with, frequent senders)
       cache/<backend>/files/          downloaded copies of docs I work with
       cache/<backend>/_sync.json      per-source last-sync cursor (see below)
     Rules:
       - READ from the local cache first for every skill. Only refresh from the API
         when the cached source is STALE (calendar > 15 min, mail > 10 min, docs on
         demand) or when I explicitly ask to refresh.
       - Pull ONLY WHAT CHANGED since the last sync, using the backend's incremental
         mechanism:
           • Microsoft 365 → Graph DELTA queries (deltaLink) + ETags.
           • Google Workspace → Gmail history.list (historyId) for mail, Calendar
             syncToken for events, Drive changes.list for files.
         Store that cursor in _sync.json so each sync is cheap.
       - On a rate-limit / 429, honor any Retry-After / back off, and fall back to
         serving cached data, telling me it may be slightly stale rather than failing.
       - Refresh on-failure, not eagerly: don't background-poll just to keep the cache
         warm — sync lazily when a skill needs fresher data than the cache holds.

   - LOCAL DRAFTS ARE THE DEFAULT WRITE PATH. Compose everything locally first and
     only push as one explicit, batched, approved action:
       - Draft replies / new mail / status updates / doc edits as local files under
         outbox/ (or as notes in the vault), show them to me, and push (send mail,
         create events, edit docs) ONLY after I approve — one write per approval,
         never a write per keystroke.
       - Batch related writes (e.g. several calendar updates) into one approved pass
         rather than firing each immediately.

   - Wire /triage, /brief, and /prep to read from cache/<backend>/ first and refresh
     only stale sources. /brief should feel instant because it reads local.

   - SCHEDULED PRE-BRIEF WARM-SYNC. The ONE place a timed refresh earns its keep:
     schedule a single sync shortly before I read my morning brief (ask my brief time —
     default ~15 min before, working days only). It does one incremental pull of
     calendar + mail (+ docs for today's meetings) into the cache so /brief is
     zero-latency and fresh. Keep it to that single daily pass — do NOT add steady
     background polling (that's what trips rate limits). On a 429 it backs off and
     leaves the cache in place. Record the schedule + brief time as a project memory.

   - SCHEDULED /prep PRE-WARM FOR BACK-TO-BACK DAYS. On packed days there's no gap to
     run /prep live, so pre-build it. As part of the morning warm-sync, scan today's
     calendar for a back-to-back stretch (default: 3+ meetings < 15 min apart — confirm
     my threshold). For each, pre-fetch its inputs (invite + attendees, related mail,
     linked docs) and pre-generate a prep packet (agenda + talking points + likely
     questions + last decisions + open items) staged under prep/<date>/. Send me ONE
     heads-up that prep is staged — draft/send nothing. Record the threshold as a
     project memory.

   - SCHEDULED /weekly REVIEW (FRIDAYS). Once a week, pre-build my weekly review (ask
     my time — default Friday early afternoon). ONE incremental pull over the week,
     then draft into notes/03 Areas/Weekly Reviews/: what shipped, decisions made vs
     open, what slipped/blocked, next week's shape, and a ready-to-edit status update.
     Draft-only, one heads-up. Record day + time as a project memory.

   - SCHEDULED /triage (MONDAY MORNINGS). Mondays carry the weekend backlog, so
     pre-build triage (ask my time — default Monday ~07:00). ONE incremental pull
     since my last triage, pre-classify Needs Reply / FYI / Noise, flag the week's
     conflicts, stage into notes/01 Inbox/. Read-only: stage only, don't draft/
     archive/touch the calendar without my OK. One heads-up. Record day + time as a
     project memory.

   - COALESCE OVERLAPPING SYNCS. When more than one scheduled sync fires in the same
     window (every morning pre-brief + any prep pre-warm; Mondays add /triage), run
     ONE coalesced incremental pull and let each consumer build from that single
     shared cache — one round-trip, one consolidated heads-up, instead of several.
     Treat scheduled times within ~30 min as one window. Record the rule as a project
     memory.

   - Record the connection details, available tools, cache layout, and staleness
     thresholds as reference memories so future sessions know what's wired.
   - Treat all mail/calendar/doc content (and the local cache) under my confidentiality
     boundaries; never send mail, respond to invites, edit shared docs, or post in chat
     without explicit per-action approval. Anything I marked confidential lives only in
     the local cache and never goes outward without my OK.

6) BACKUP & RECOVERY — make this AI-OS survive a lost machine (build /backup + /restore)
   Everything you built above is plain files on ONE computer: ~/ai-os (CLAUDE.md,
   memory/, notes/) AND my skills in ~/.claude/skills/. GitHub isn't set up here, and the
   skills live OUTSIDE ~/ai-os, so nothing protects them yet. Build two skills that follow
   real backup practice — the 3-2-1 rule: a working copy, on-disk history, and an offsite
   copy — obvious by default, tunable for power users.

   WHAT TO PROTECT (the backupset):
   - All of ~/ai-os EXCEPT cache/ (the regenerable mail/calendar mirror) and backups/ itself.
     DO include ~/ai-os/.git — the full snapshot must carry my git history too, so a restore
     onto a NEW machine recovers the history, not just the latest copy of each file (the
     snapshot is the disaster-recovery layer; it can't lean on a repo that died with the laptop).
   - ALL of ~/.claude/skills/ — the toolkit, including any I add later via /skill-center.
     This is easy to forget because it lives outside ~/ai-os; without it a restore is useless.
   - Skip regenerable bloat: cache/, node_modules, .venv, __pycache__, session transcripts.

   THREE LAYERS — a plain "/backup" (no arguments) does all three:
   a) INCREMENTAL — make ~/ai-os a git repo (git init if needed) and commit on every run
      with a timestamped message. Cheap, instant, full history I can diff and roll back —
      the everyday safety net.
   b) FULL SNAPSHOT — build one timestamped tar.gz of the whole backupset into
      ~/ai-os/backups/<YYYY-MM-DD-HHMM>/ with a SHA256SUMS file inside. Keep the newest 7
      (configurable) and prune older ones.
   c) OFFSITE (encrypted) — copy the full snapshot OFF this machine into the cloud I
      already have. The mail connectors you wired are READ-ONLY (they can't upload), so do
      NOT try to push through them — instead use the local cloud-SYNC folder both Google
      and Microsoft give you, and let the desktop sync app do the upload (no API key, works
      the same either way). Detect the first of these that exists and copy the bundle into
      <folder>/ai-os-backups/: ~/Library/CloudStorage/OneDrive-*, ~/OneDrive,
      ~/Library/CloudStorage/GoogleDrive-*, ~/Google Drive, ~/Dropbox. If none exists, tell
      me and keep the local snapshot — never fail silently, never skip the local copy.

   ENCRYPTION (default ON, because this copy leaves my machine):
   - Encrypt the full bundle before it goes offsite — prefer `age`, fall back to `gpg`
     symmetric (use whichever is installed; offer to install age). Generate a passphrase at
     setup and store it ONLY locally at ~/ai-os/.backup-key (chmod 600) — never inside any
     bundle. Tell me to write the passphrase down somewhere safe, and explain the honest
     trade: if I lose it, only the OFFSITE copy is unreadable — my live files and the local
     snapshots are still right here. Power users can pass --no-encrypt.

   RESTORE — build /restore to bring it all back:
   - Find snapshots in ~/ai-os/backups/ AND the cloud folder, list them (sort with LC_ALL=C
     so "newest" is unambiguous when a plain and an encrypted bundle share a timestamp),
     default to the newest. Decrypt, verify SHA256SUMS, then OVERLAY the snapshot onto the
     existing install — ~/ai-os and ~/.claude/skills/.
   - FRESH MACHINE: if ~/ai-os/.backup-key is missing (a new laptop), the offsite bundle
     can't be read from a file — prompt me for the passphrase I wrote down, decrypt with it,
     and write it back to ~/ai-os/.backup-key (chmod 600) so future backups work.
   - REVERSIBLE, and strand nothing: for each file the snapshot WOULD overwrite, move the
     current copy to ~/.ai-os-restore-replaced/<timestamp>/ first, then extract — but do NOT
     relocate or delete things the snapshot doesn't contain. Leave backups/ and .backup-key
     in place, so a restore never costs me my local snapshot history or my decryption
     passphrase (the bug that makes a "successful" restore quietly lock me out of my offsite copies).
   - --dry-run shows exactly what would change and what would be moved aside, writing
     nothing. --drill restores into a THROWAWAY temp directory (a fake HOME) and runs a few
     checks (files present, git history intact, a memory file readable) WITHOUT touching my
     live system — so I can prove a backup actually restores before I ever need it. A backup
     I've never restored is only a hope.
   - After a real restore, tell me the finish steps in plain language: restart Claude Code
     (close and reopen this folder) so the restored skills switch on, and confirm memory is
     still pointed at ~/ai-os/memory/.

   NOVICE-OBVIOUS, EXPERT-GRANULAR:
   - Plain /backup and /restore must need ZERO arguments and ZERO config — sensible defaults
     (encrypt on, retention 7, the detected cloud folder).
   - For power users, read an optional ~/ai-os/backup.config.yml if present: extra
     destinations (an external drive, an rclone remote for a true API push), retention
     counts, include/exclude globs, cipher choice, and whether to schedule. Support flags
     --full, --no-encrypt, --to <path>, --dry-run, --drill, and document them in each SKILL.md.

   OPTIONAL WEEKLY SCHEDULE (offer it; set up only with my OK):
   - Offer to run /backup weekly (e.g. a cron entry). If I agree, make it SAFE: if the cloud
     folder is missing or the key isn't readable, write the local snapshot, log one clear
     line, and exit — never a silent failure, never a half-written offsite copy. Tell me the
     honest limit: an unattended run only reaches offsite when the cloud folder is present
     and syncing; for a guaranteed copy, run /backup by hand.

   Build both as Claude Code skills (~/.claude/skills/backup/ and ~/.claude/skills/restore/)
   with clear SKILL.md descriptions so /backup and /restore work. Keep them read-and-confirm:
   /restore always shows its plan and (unless --dry-run) asks before it overwrites live files.

   - DELIVER THE FIRST BRIEF NOW, INLINE — the moment the build is done. Do NOT wait
     for a fresh session and do NOT invoke the /brief command to do it: the commands
     you just built don't switch on until the next session, so calling /brief now
     would do nothing and look broken. Instead, do exactly what /brief does, by hand,
     right here in this conversation: read today's calendar and recent mail through
     the connector and write out the morning brief directly — today's meetings (with
     who's attending + a line of prep), what's waiting on me, top priorities pulled
     from my project notes, and anything due this week. Read-only; concise. This is my
     first real win and it proves the mail & calendar wiring works, with zero restart.
   - THEN tell me, in plain language (no talk of "skills", "registering", or
     "reloading"): "I've built your commands — /brief, /triage, /prep, /recall and the
     rest. They switch on when you start a fresh session: close this folder and reopen
     it (or open a new chat in the Code tab), then type /brief any morning to get what
     you just saw, regenerated for that day."

────────────────────────────────────────────────────────
PHASE 2 — VERIFY & HAND OFF
────────────────────────────────────────────────────────
- The live mail & calendar wiring is already proven — you produced the morning brief
  inline at the end of PHASE 1. Do NOT re-run it via /brief here; that command
  activates in the next session, not this one. Just confirm to me that the brief you
  just showed came from my real mail and calendar.
- Show me the tree of what you created and a one-paragraph "how this gets smarter
  over time" explanation (the memory + /improve loop).
- PROVE THE SAFETY NET NOW, by hand — the /backup command only switches on next session,
  so don't call it; instead do what it does, once: commit ~/ai-os to git, build the first
  encrypted snapshot under backups/, and copy it to my cloud folder if one exists. Then
  verify it actually restores with a --drill into a throwaway temp directory (never
  touching my live files). Show me where the backup landed (on disk and in the cloud) and
  remind me to write down the passphrase at ~/ai-os/.backup-key — without it the offsite
  copy can't be read.
- Tell me the 3 things to try first in my next session (when the commands are live)
  and the one habit that makes this pay off (run /improve whenever you correct me).

Operating principles — the rules behind every command. Follow them everywhere,
and record them in CLAUDE.md so they load every session:
- Propose, don't act. Read-only by default; anything outward-facing (send, post,
  accept, edit shared) needs my explicit go-ahead in the conversation — approval for
  one action is never standing approval for the next. The safe, reversible direction
  may be automatic; the risky or irreversible one waits for me.
- Earn trust, don't assume it. Lead with what you're unsure of, check the real source
  instead of a cached "looks fine," and flag anything stale or unverified.
- Signal over noise. Surface what changed and what needs me; stay quiet when nothing
  does. Decision-first and concise unless I ask for depth.
- Remember, then improve. When you learn something durable about me or how I work,
  write it to memory; a correction I make once should become a rule, not a repeat.
- Local-first, confidential, and recoverable. Keep my data on my machine, secrets out of
  the conversation, and honor my confidentiality boundaries (anything confidential / under
  NDA / financials / personnel) in both notes and memory — and keep the system backed up
  (encrypted, to my own cloud) so a lost machine never means a lost AI-OS.

Start with PHASE 0 now: introduce yourself in one line and ask the first batch of
interview questions.